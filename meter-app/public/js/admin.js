// JavaScript สำหรับ Admin Dashboard
document.addEventListener('DOMContentLoaded', () => {
    console.log('Admin Dashboard Loaded');
    // เพิ่มฟังก์ชันการทำงาน เช่น การโหลดข้อมูลผ่าน AJAX
});
